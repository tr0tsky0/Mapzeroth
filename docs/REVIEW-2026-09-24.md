# Mapzeroth Rebuild — Cruft Review, 2026-09-24

Status: batches 1 to 5 are committed on `rebuild` (not pushed). Finding 4b is dropped, 3 is done in full, and 6 is done in code: Modern is on Forever's schema. What Modern's data still lacks is listed under "Modern data to gather" in the batch 5 log. Batches are picked from the "Order of attack" at the end;
each batch ships with an in-game checklist for both flavours.

Scope: `Mapzeroth-Rebuild` (the two-toc addon). The old shipped addon under `../Mapzeroth/` and
the `tools/MapzerothDataTools` dev addon are reference only, except where the rebuild depends on
them. Baseline at review time: the default Forever test run plus the Modern smoke and picker tests
all pass.

## Architecture

**The two .toc files differ only in the data block.** Header lines (Interface, Title, Notes) and
comments differ, then Forever lists `Data\Forever\*` and Modern lists `Data\Modern\*`. The
Constants, Locales, Options, engine, UI and Core lists are identical and in the same order. No
other divergence. Forever's toc includes an empty placeholder `Geometry.lua`, which is harmless.

**File responsibilities, in load order.**

- `Constants.lua`: tuning numbers, plus some flavour-specific tables that have drifted in (finding 8).
- `Locales.lua` / `Locales_UI.lua` / `Locales/*.lua`: the string table with client-locale then English fallback.
- `Options.lua`: settings definitions, clamping, change listeners, backed by `MapzerothRebuildDB.settings`.
- Data files: `addon.Nodes.<group>`, `addon.Edges`, `addon.Containers`, `addon.Abilities`, plus
  Forever-only `Cities/Towns/Professions/ProfessionRanks/ClassWeapons/RidingSkills/PathFactors/LeyLineSpell`
  and Modern-only `CityPlaces/Instances/InstanceNodeAliases/CURRENT_EXPANSION/SEASONAL_DUNGEONS/DEFAULT_MOUNT_BONUS`.
  Both ship `Geometry`.
- `World.lua`: container tree from dotted paths, inherited flags, map-to-container lookup.
- `NodeNames.lua`: name resolution from client APIs, kind patterns and locale strings, cached per node.
- `Hearth.lua`, `FlightKnowledge.lua`: the two pieces of live per-character state (bind location,
  found flight points and fare factors).
- `Trainers.lua`, `Relevance.lua`: picker relevance rules.
- `PlayerAbilities.lua`: the player context snapshot and the "anywhere to node" teleport list.
- `Equipment.lua`, `MovementSpeed.lua`, `EdgeRequirements.lua`: equip/restore state machine, ground
  and flight speed, requirement checkers.
- `TravelGraph.lua`: adjacency list per context: authored edges filtered by requirements, cached
  static geometry (walk, gate, fly), teleports, plus start and waypoint injection.
- `Pathfinder.lua`: label-setting Dijkstra with phase state, ticket-in-the-air state and optional
  fare budget; step collapsing.
- `Search.lua`, `Destinations.lua`, `Sections.lua`: the picker's entries, search scoring, accordion layout.
- `Location.lua`, `Journey.lua`: player start and waypoint; session, pricing, plan, readable steps, hints.
- `Navigation.lua`, `MapRoute.lua`: frame-free trip tracking and route geometry.
- `UI/`: Theme (the only styling layer), RouteLines and MinimapLines (drawing), Panel (docked
  picker), Navigator (trip window), OptionsPanel.
- `Core.lua`: events, login init, slash command, the `MapzerothAddon` global for the dev tool.

**Data flow.** Login: `World:Build` → `FlightKnowledge:Load` → `Theme:Init` → options page →
`Panel:Init`. Map open: `GetPlayerContext` → `Destinations:Build` → `Sections:Build`. First section
opened or a pick: `GetPlayerStart` → `Journey:Build` (→ `TravelGraph:Build` → `AddStart`) →
`Pathfinder:FindCosts` or `FindPath` → `CollapseSteps` → readable steps. Start: `Navigation:Start`
→ Navigator ticks `Sample`/`Update` → `Render` feeds RouteLines, MinimapLines, Equipment and the
panel highlight.

**Hot paths.**

- Navigator `OnUpdate` every 0.5 s: `Navigation:Update` (several world-position projections per tick) and `Render`.
- MinimapLines `OnUpdate` every 0.1 s while a trip runs: a full redraw with projections and line pooling.
- Panel `OnTextChanged`: `Search:Query` over every entry per keystroke, then `Render`.
- Per destination click: two full `TravelGraph:Build` calls and two or three Dijkstra runs (finding 4).
- Per map open: `Destinations:Build` over every node with a `C_Map.GetMapInfo` call each, plus name folding.
- `Pathfinder.run` inner loop: one string key concatenation and, when not dominated, one label table per relaxed edge.

## Findings, ranked by impact

Each finding: location, what/why, fix, flavours, risk, effort.

### 1. Hearthstone bind is saved account-wide but is per character

- Where: `Hearth.lua:12-19`, written from `Core.lua:42-50`.
- What: `SaveBind` writes `MapzerothRebuildDB.hearthstone` with no character key, while flights and
  fares are keyed per character. The second character to bind overwrites the first, so
  `GetBoundInnNode` returns another character's inn and hearthstone routes go to the wrong place.
- Fix: store under `MapzerothRebuildDB.hearthstones[characterKey]`, sharing the key helper with
  FlightKnowledge; read the old `hearthstone` field once as a migration fallback, then delete it.
- Flavours: both (Modern can't resolve an inn yet, but the bug is in shared code).
- Risk: low with the migration. Effort: trivial.

### 2. Holiday-gated edges can never open on Modern, for two independent reasons

- Where: `Constants.lua:66-70`, `PlayerAbilities.lua:64-82`, `Data/Modern/Edges.lua:224-229`.
- What: the data says `holiday = "Darkmoon Faire"` but the table is keyed `darkmoon_faire`, so the
  lookup is nil and the result is cached false for the session. Separately, the old addon waited for
  `CALENDAR_UPDATE_EVENT_LIST` before trusting `C_Calendar.GetNumDayEvents` (it returns nothing until
  the calendar has loaded); the rebuild dropped that and caches the first answer. The key mismatch
  existed in the old addon too (inherited), but the lost calendar priming is a regression.
- Fix: normalise the key in the generator (data side, preferred) or in the checker; register the
  calendar event in Core, call `C_Calendar.OpenCalendar` once, and don't cache a negative before the
  calendar is ready.
- Flavours: Modern (Forever has no holiday edges yet; the calendar fix is shared).
- Risk: low. Effort: trivial.

### 3. Phase-split containers are porous: the fly mesh and the phaseswitch edges ignore them

- Where: `TravelGraph.lua:181-211`, `Data/Modern/Edges.lua:269-275`, `Data/Modern/Containers.lua`.
- What: the `_art<N>` container suffix only stops walk auto-edges. The continent-wide fly pass joins
  every flyable node within range regardless of container, so `TIRISFAL_ZIDORMI_PAST` and friends get
  fly edges from ordinary nodes (visible in the shipped Geometry). The seven `phaseswitch` edges are
  live with no `phaseGroup` on any container, so Zidormi is a free ten-second hop between the two
  states. Meanwhile the `mapArtID` requirement has no checker, so those 18 edges fail closed as
  intended. Net effect: routes can enter a phase the player isn't in, then dead-end. This is the
  known-open phase item, but the fly-pass hole isn't in the notes.
- Fix: short term, have the generator put the same inert `mapArtID` gate on the phaseswitch edges,
  and have the fly pass skip nodes whose container path carries `_art` (or better, a real
  `phaseGroup`) until the phase model lands. Long term, the fly pass must respect `phaseGroup` like
  the walk pass respects containers.
- Flavours: Modern.
- Risk: low for the short-term gate; medium for the design change. Effort: trivial / significant.

### 4. Every destination click builds the whole graph twice and allocates tens of thousands of edge tables

- Where: `TravelGraph.lua:242-346`, `Journey.lua:186-195` and `Journey.lua:214-227`.
- What: `Build` copies every static geometry triple into a fresh `{from,to,cost,method,...}` table
  per call. On Modern that is roughly 40 to 60 thousand tables. `Panel:ShowRoute` creates a new
  session per click, and `flightHint` (and `missingFlightHint`) then calls `Journey:Build` again
  with a modified context to compute the "free" route, so it is two full builds plus up to three
  searches per click. Also `authored` (lines 280-283) rebuilds a string-keyed set every call though
  it is context-independent.
- Fix, in two steps: (a) stop dropping unfound taxi edges in `Build`; tag them `unfound = true` and
  give the search an `opts.ignoreUnfound`, so the hint runs on the same graph with no second build.
  Cache `authored` per `World.generation`. (b) later, let the search read the static triples directly
  with a per-container speed table instead of materialising them per build.
- Flavours: both (Modern feels it; Forever's graph is small).
- Risk: (a) low, tests cover hints; (b) medium, changes step shape through Pathfinder and Journey.
  Effort: (a) moderate, (b) significant.

### 5. Per-tick allocations and repeated projections while following a trip

- Where: `Navigation.lua:62-66`, `Navigation.lua:81-89`, `Navigation.lua:344-366`, `UI/MinimapLines.lua:167`.
- What: `distance()` builds a throwaway node table and does two client projections per call, and
  `Update` calls it for every later step (the skip loop), the jump check, and the current step,
  every 0.5 s. `yardsPerUnit` allocates three tables and does four projections per call; `Heading`
  calls it every tick and MinimapLines calls it every redraw at 10 Hz, though the answer per map
  never changes. `Sample` allocates a closure per tick and `active.last` a table per tick.
- Fix: project the sample to world coordinates once per `Update` and compare against cached node
  world positions; memoise `yardsPerUnit` per mapID; reuse the `last` table.
- Flavours: both. Risk: low. Effort: moderate.

### 6. Schema drift: two city models, two instance-naming schemes, two node-id conventions

- Where: `Destinations.lua:99-132` vs `Destinations.lua:179-193`; `NodeNames.lua:213-245`; `Sections.lua:49-97`.
- What: Forever describes a city as `Cities[key] = { mapID, x, y, taxi, area, faction }` with
  `CITY_<KEY>` centre nodes and entrance pairs; Modern describes it as
  `CityPlaces = { maps = {...}, expansion, hub }`. Forever instances are `INSTANCE_<NAME>` with
  `kind = "instance"` and `area`; Modern's are `INSTANCE_<journalID>` plus `Instances` and
  `InstanceNodeAliases`. Forever ids are `<KIND>_<PLACE>`, Modern's `<PLACE>_<KIND>`, so the
  prefix-based name fallback and the `TRANSPORT` prefix set in `groupOf` work only for Forever
  (Modern docks and portals all become group "other"). Shared code carries both shapes side by side;
  `addSettlements` and `addCityPlaces` are each a no-op on the other flavour.
- Fix: one city shape (`Cities` entries gain optional `maps`, `expansion`, `hub`; Modern's
  Places.lua emits that), one instance reference (`GetInstanceRef` already abstracts it; make
  Forever's `kind = "instance"` nodes carry a journal id or alias too, if Forever's client has the
  Encounter Journal, which is unconfirmed), and a suffix fallback next to the prefix one in NodeNames
  and `groupOf`.
- Flavours: both (code), Modern (data). Risk: medium, touches picker output. Effort: significant.

### 7. Flavour branching in shared code, keyed on data presence

- Where: `Sections.lua:116` and `Sections.lua:158-163` (`addon.CURRENT_EXPANSION`),
  `MovementSpeed.lua:24` (`not addon.RidingSkills`).
- What: no `if Forever then` exists, which is good. But two places pick a code path by whether a
  flavour-specific table happens to be loaded. That is the data-layer approach in spirit, yet the
  intent is inferred rather than declared, and a Forever dataset that later gains
  `CURRENT_EXPANSION` would silently switch pages.
- Fix: declare it in data: `addon.PickerLayout = "expansions" | "settlements"` in each flavour's
  Places/Pois file, and `addon.DEFAULT_MOUNT_BONUS = nil` explicitly in Forever with the rule
  "riding data wins when present" documented once.
- Flavours: both. Risk: low. Effort: trivial.

### 8. Flavour-specific data living in shared Constants

- Where: `Constants.lua:31-34` (`CLASS_TOKENS`, Forever's nine classes), `Constants.lua:66-70`
  (`HOLIDAYS`, Modern only), `Constants.lua:78` (`FREQUENT_FLIER`, Forever only).
- What: `CLASS_TOKENS` is missing Modern's four classes, which affects trainer naming and the
  validator (latent, since Modern has no trainer data yet). The others are inert on the wrong
  flavour but belong with their data.
- Fix: move all three into `Data/<flavour>` files and let the engine treat them as optional, as it
  already does for `LeyLineSpell`.
- Flavours: both. Risk: low. Effort: trivial.

### 9. The same Hearthstone item is a different method per flavour

- Where: `Data/Forever/Abilities.lua:46-48` (`method = "hearthstone"`) vs
  `Data/Modern/Abilities.lua:122-125` (no method), `PlayerAbilities.lua:153`.
- What: Modern's entry defaults to "teleport", so the step reads "Use Hearthstone" instead of "Use
  your Hearthstone to go to X". Cosmetic, but it is schema drift on item 6948 in both datasets.
- Fix: default the `Hearthstones` list's method to "hearthstone" in `GetKnownTeleports` (shared),
  or have the generator emit it.
- Flavours: Modern. Risk: low. Effort: trivial.

### 10. Two layered flight-knowledge predicates

- Where: `PlayerAbilities.lua:102-109`, `TravelGraph.lua:270-274`, `Journey.lua:199-210`, `Journey.lua:255-259`.
- What: `flightNodeFound` was the original strict rule; `flightUsable` was added on top for the
  "assume found" setting, and every consumer now branches on which one the context has. Tests build
  contexts with neither to mean "no rule". This is a patch on a patch.
- Fix: make the context always carry both, with `flightUsable` the single routing predicate and
  `flightNodeFound` the raw fact for hints; give the test `makeCtx` an explicit `flights = "all"`
  mode; delete the `if ctx.flightUsable then ... else` branches.
- Flavours: both. Risk: medium (test contexts change). Effort: moderate.

### 11. Dead code and unread data fields

- `Pathfinder.lua:153-155` `airKey` is never called; `relax` builds the key inline.
- `Journey.lua:102` method `racial` exists nowhere in data or the other method tables.
- `UI/RouteLines.lua:197-212` `Show`, `SetCurrent`, `Clear` and `UI/MinimapLines.lua:229-231`
  `Clear`: only `Follow` is used by the addon.
- `Journey.lua:59-61` `Cost` and `Journey.lua:78-81` `EntryCost`, `UI/Theme.lua:233-235` `Strip`:
  no callers anywhere.
- `Destinations:Find`, `World:GetContainer`, `FlightKnowledge:Reset`, `GetSpellLabel`: test-only.
- `Constants.lua:80-92` `GetRuleset` and the threshold: the design moved to per-toc data, and only
  the dev tool's `/mzr world` calls it. Keep as a tool-facing API or move it into Dev.lua (user's call).
- Data fields `cooldown` (both flavours, every ability) and `bind` (Forever hearthstone) are never
  read; routing uses live cooldowns. Either drop them from the generator and file, or document them
  as notes.
- Defensive fallbacks on constants that always exist (`addon.FLY_SPEED or 50` three times,
  `FLIGHT_CHAIN_SAVING or 0`, `PROFESSION_TRAINER_REACH or 2`, `DEFAULT_LOADING_SCREEN_TAX or 10`):
  they hide a missing constant instead of failing loudly.
- Fix: delete, or move test-only helpers into the harness prelude.
- Flavours: both. Risk: low. Effort: trivial.

### 12. Duplicated helpers

- Item cooldown: `PlayerAbilities.lua:46-55` and `Equipment.lua:94-103` are the same function with
  a different GCD guard (`<= 1.5` vs `== 0`), so the navigator's "ready in" text and the planner can disagree.
- "Walk up to the continent": `NodeNames.lua:29-37`, `Destinations.lua:89-97`,
  `FlightKnowledge.lua:181-193`: three copies, one unguarded `Enum` use, one guarded, one literal `2`.
- Spell or item display name: `Journey.lua:86-98` vs `UI/Navigator.lua:157-169`; the navigator copy
  uses the deprecated `GetItemInfo` path only.
- Saved window position: `UI/Panel.lua:562-584` vs `UI/Navigator.lua:33-57`.
- "Flyable outdoor node" predicate three times and `insideCity(a) == insideCity(b)` four times in
  TravelGraph; a `flyableNodes` list could be cached with the static geometry so
  `AddStart`/`AddDestination` stop walking the tree per node per call.
- Theme default "moderndark" in `Core.lua:54` and `Options.lua:16`.
- Fix: one helper each, in the file that owns the concept.
- Flavours: both. Risk: low. Effort: trivial to moderate.

### 13. The travel-method vocabulary is spread over five tables

- Where: `Navigation.lua:36-40` `KINDS`, `MapRoute.lua:18-22` `STYLES`, `Journey.lua:102`
  `ABILITY_METHODS`, `Constants.lua:61` `DEFAULT_LOADING_SCREENS`, and `methods` in each theme.
- What: `transition` is in two of them, `equip` in three, `racial` in one. A new method (the
  proposed merged "manual" method) means touching all five.
- Fix: one `addon.METHODS = { walk = { kind = "walk", style = "foot", screens = 0, ability = false }, ... }`
  in Constants; the others derive from it. Theme colours can stay per theme but keyed by `style`.
- Flavours: both. Risk: low. Effort: moderate.

### 14. Duplication inside the Modern data folder

- Where: `Data/Modern/Edges.lua:18` and `:26`; `Data/Modern/Abilities.lua:146` and `:165`.
- What: `STORMWIND_EXODAR_PORTAL → EXODAR` appears twice, once bidirectional and once one-way, and
  the return is also authored on line 27, so the search sees three edges for one portal pair. The
  Darkmoon pairs on lines 224-227 look intentional (one per faction). The two Garrison Hearthstone
  entries carry no `faction`, so each player is offered both garrisons. Nothing is copy-pasted
  between the two folders that a shared file would serve better.
- Fix: dedupe in `gen_modern_edges.py` (exact from/to/method) and add the factions in `modern_manual.py`.
- Flavours: Modern. Risk: low. Effort: trivial.

### 15. Non-English locales are silently 19 keys behind, and the test can't see it

- Where: `Locales/*.lua`, `tests/test_locales.lua:4-30`.
- What: every locale file lacks the same keys added since it was written: `HINT_ASSUMED`,
  `HINT_ONLY_FLIGHT_UNFOUND`, `HINT_ONLY_FLIGHT_UNKNOWN`, `NAV_ITEM_READY_IN`, `NODE_PORTAL_ZONE`,
  `OPT_ASSUME_FLIGHTS`, `OPT_ASSUME_FLIGHTS_DESC`, `ROUTE_MORE_ABOVE`, `ROUTE_MORE_BELOW`,
  `SECTION_DUNGEONS`, `SECTION_EXPANSION`, `SECTION_OLDER`, `SECTION_RAIDS`, `STEP_CAST`,
  `STEP_EQUIP_ITEM`, `STEP_PORTAL_TO`, `STEP_USE_ITEM`. `PANEL_TITLE`, `OPT_TITLE` and the
  `SKILL_ALIAS_*` keys are correctly absent. The test checks placeholder shape only, not completeness.
- Fix: add the starter lines, and a completeness check with a small allowlist for brand and alias keys.
- Flavours: both. Risk: none. Effort: trivial.

### 16. Validator gaps that would have caught findings 2, 3 and 14

- Where: `tests/Validator.lua:69-102`.
- What: it doesn't check `holiday` values against `HOLIDAYS`, doesn't validate `toList`
  destinations (only `to`), doesn't report exact duplicate edges, and doesn't warn when a
  `phaseswitch` edge joins containers with no `phaseGroup`.
- Fix: four small checks. Flavours: both. Risk: none. Effort: trivial.

### 17. WoW hygiene items

- `UI/Navigator.lua:199-215` and `:304-312`: `ui.frame:Hide()` can run in combat (the "arrived"
  auto-close and the off-route stop). The frame is the parent of a `SecureActionButtonTemplate`
  button. Hiding an unprotected parent is believed to be permitted, but unconfirmed on Forever's
  client; worth a combat test. If blocked, defer the hide to `PLAYER_REGEN_ENABLED`.
- `UI/MinimapLines.lua:101-103`: once the frame exists, `refreshIndoors` nudges the minimap zoom on
  every zone change for the rest of the session, trip or not. The nudge mirrors Blizzard's own
  indoor detection so it is a legitimate quirk workaround, but the events should be unregistered
  while no trip is followed.
- `TravelGraph.lua:16-28`: `worldPosCache` keeps an entry for every distinct rounded start id ever
  used and is never cleared. Skip caching for `YOU_` and `WAYPOINT_` ids.
- `Core.lua:18-64`: the event handler is an if-chain whose final block is implicitly
  `PLAYER_LOGIN`; any newly registered event that isn't handled above will rebuild the world. Make
  the login branch explicit.
- `UI/OptionsPanel.lua:135-142`: the `InterfaceOptions_AddCategory` fallback is dead on both clients
  (both have the Settings API). Keep or drop (user's call, Forever's client is a beta).
- No leaked globals found. The only globals are `MapzerothAddon`, the slash-command names,
  `MapzerothRebuildDB` and three named frames. Events are lifetime-registered by design. Frames are
  pooled or built once everywhere.
- Flavours: both. Risk: low. Effort: trivial.

### 18. `Destinations:Build` runs on every map open and does per-node client calls

- Where: `Destinations.lua:141-171`, `UI/Panel.lua:599-606`.
- What: `GetZoneName` calls `C_Map.GetMapInfo` per node (uncached, unlike node names), then
  `Search:Prepare` folds every name with three `gsub` passes. On Modern that is over a thousand
  nodes per map open.
- Fix: cache zone names per mapID in NodeNames; rebuild entries only when faction, class or
  known-spell signature changes.
- Flavours: both. Risk: low. Effort: moderate.

### 19. Structure nits

- `UI/Panel.lua:311-320` prices sections with the context captured at map open, while
  `UI/Panel.lua:383-384` takes a fresh one per route; money and cooldowns can differ between the
  list and the plan. Take a fresh context in `PriceSections`.
- `Journey.lua:244-246` repeats `(chosen or fastest)` five times; bind it once.
- `UI/Panel.lua:177-181` `showList(true)` does nothing; rename to `hideList`.
- `UI/Theme.lua:143-147` allocates the `slots` table on every hover; hoist it.
- Flavours: both. Risk: none. Effort: trivial.

### 20. Forever data never sets `indoor = true` anywhere

- Where: `Data/Forever/Containers.lua:16` is the only entry.
- What: the Wizard's Sanctum and Blackrock Mountain interior containers inherit `indoor = false`,
  so mounted speed applies inside them. Data, not code, but the engine's indoor logic is untested on
  Forever as a result.
- Fix: add `indoor = true` for the two Forever interior containers.
- Flavours: Forever. Risk: low. Effort: trivial.

### Unsure, not guessed

- The dev tool's `Dev.lua` line 262 calls `a:GetContinentMapID`, which no longer exists in the
  engine (guarded, so it silently does nothing). Outside this repo, but stale.
- Whether Forever's client exposes the Encounter Journal (`EJ_GetInstanceInfo`) decides how far
  finding 6's instance unification can go.
- The minimap zoom nudge (finding 17) is a Blizzard-quirk workaround copied from Blizzard's own
  code; keep it.
- The `_Mainline` toc keeping Forever from loading Modern data is still unconfirmed against the
  live client; not a code finding.

## Order of attack

Ship together, safest first. Each batch gets an in-game checklist for both flavours when done.

1. **Quick wins, no behaviour change:** dead code (11), duplicate helpers with the cooldown guard
   unified (12), Modern duplicate edge and garrison factions (14), locale starter lines plus
   completeness test (15), validator checks (16), Core login branch and `Enum` guard consistency
   (17), structure nits (19), Forever indoor flags (20).
2. **Behaviour-changing bug fixes, flagged:** per-character hearth bind with migration (1), holiday
   key plus calendar priming (2), Hearthstone method default (9), phaseswitch gating and fly-pass
   exclusion for `_art` containers (3, short-term part).
3. **Performance:** single graph build per click via `ignoreUnfound` and cached `authored` (4a),
   projection caching in Navigation and MinimapLines (5), zone-name caching and entry reuse (18).
4. **Structure:** method vocabulary table (13), flight predicate consolidation (10), flavour tables
   out of Constants (8), declared picker layout (7).
5. **Design-level, needs a decision first:** city and instance schema unification (6), fly pass
   honouring phase groups (3, long-term part), static geometry shared with the search (4b).

## Batch log

### Batches 1 and 2 — 2026-09-24

Done, one commit per finding on `rebuild` (not pushed): 11, 12, 14, 15, 16, 17, 19, 20 (batch 1); 1, 2, 9, 3 short-term
(batch 2). Both test runs green after every commit: `python tests/harness.py` (29 files) and
`python tests/harness.py --toc Mapzeroth-Rebuild_Mainline.toc modern_smoke modern_picker`.

**Where the work differed from the brief, and why**

- **11:** `Journey:Cost` is not dead: `tests/test_journey.lua` calls it (through the `J` alias, which the review's grep
  missed). Kept, with a "Tests only." comment. `Journey:EntryCost`, `RouteLines:Show/Clear`, `MinimapLines:Clear`,
  `Theme:Strip`, `airKey`, `racial` and the dead `or <literal>` fallbacks are gone. `skin.strip` in Theme.lua is now
  unreferenced (only `Theme:Strip` used it); left alone as not in the brief.
- **12:** `Equipment:CooldownLeft` now goes through `addon:ItemCooldownRemaining` (the `<= 1.5` rule), so the navigator's
  "ready in" text and the planner agree. `addon:GetContinentMapID` (NodeNames.lua) replaces the three continent walks
  (the literal `mapType == 2` is gone). `addon:GetAbilityLabel` (Journey.lua) is used by Navigator's `configureUse`; one
  small difference: the navigator now also tries `C_Item.GetItemNameByID` first and asks the client to load an unknown
  item, as the route text already did. `isFlyable(node)` is in TravelGraph.lua (no cached flyable list; that is batch 3).
  Theme default now comes from `Options:Default("theme")`. Saved window position left as two functions (different DB keys).
- **14:** the duplicate check keys on from, to, method **and requirements**, not from/to/method alone. On the literal
  rule the Darkmoon Faire pairs (one edge per faction, same ends and method) would have been dropped and Horde could no
  longer enter. Only `STORMWIND_EXODAR_PORTAL -> EXODAR` (the oneway repeat of the bidirectional edge) is dropped; it
  is listed in `Data/Modern/CONVERSION_NOTES.md`. The Garrison Hearthstones got their factions through a new
  `ABILITY_FACTIONS` dict in `tools/modern_manual.py`, read by `gen_modern_abilities.py`. Node count unchanged (1233);
  the Nodes files did not change.
- **15:** the completeness test found two more keys the review missed, `KIND_dungeon` and `KIND_raid`; added to every
  locale as well. The test uses a third argument, `RegisterLocale(locale, tbl, uiStrings)`, set by Locales.lua and
  Locales_UI.lua, and `addon:GetUIStringKeys()`. All starter translations are machine quality.
- **16:** (b) `toList` only exists on abilities (no edge has one), so the check is on abilities, mirroring their `to`
  check. (c) uses the same from/to/method/requirements key as finding 14. (d) does not warn for a phaseswitch edge that
  carries a `mapArtID` gate (closed, so not a live hazard); without that exemption it would still fire after finding 3,
  because gating the edges does not give the containers a `phaseGroup`. Forever validates clean.
  **Modern validator, at the end of batch 2:** the holiday, duplicate and phaseswitch findings are gone. What is left is
  the 25 `unknown requirement 'mapArtID'` errors (18 that were there before, plus the 7 phaseswitch edges gated now;
  these fail closed by design until the phase model exists) and the "no name string" warnings, which were there before.
- **17:** the `MINIMAP_UPDATE_ZOOM` / `ZONE_CHANGED_NEW_AREA` events are registered while a plan is followed and
  unregistered when it clears; `refreshIndoors` is unchanged. World positions of `YOU_`/`WAYPOINT_` ids are no longer cached.
- **20:** no test number changed (test_walls and test_walking are unaffected).
- **2:** apostrophes are dropped, not turned to underscores, when normalising ("Feast of Winter's Veil" must become
  `feast_of_winters_veil`). Only "Darkmoon Faire" exists in the data now; an unknown holiday stops the generator.
- **9:** the test is in tests/test_hearth.lua (that is where the hearthstone route is built), not test_equipment/test_journey.
- **3:** the seven phaseswitch edges get `mapArtID = { mapID, artID }` from their from-node's container
  (e.g. Tirisfal past `{ 18, 19 }`). The fly pass, `AddStart` and `AddDestination` skip nodes whose container path
  contains `_art` (in `isFlyable`, marked temporary). `tools/strip_geometry_phase_fly.py` removed 830 fly entries from
  the shipped `Data/Modern/Geometry.lua` (node count untouched, safe to re-run). The new smoke test scans both the
  shipped and the live-computed fly mesh, and I confirmed it fails without the fix.

**Deferred on purpose**

- Navigator hiding its frame in combat (finding 17): needs a combat test in game.
- The `InterfaceOptions_AddCategory` fallback in OptionsPanel (finding 17): left for Forever's beta.
- A shared saved-window-position helper (finding 12): not worth it.
- **Re-run `/mzr dumpgeometry` in the retail client** at the next opportunity: the shipped geometry was patched by
  script, not re-dumped.
- Batches 3 to 5 (findings 4 to 8, 10, 13, 18 and the design-level items) not started.

**Forever test numbers changed by finding 20:** none.

### In-game checklist for batches 1 and 2

Each line: do this, expect that.

#### Forever (WoW Forever, Interface 16001)

*Login and settings*
- Log in with the addon enabled: no Lua errors, and the panel appears when the world map opens.
- `/mz settings`: the settings page opens; the theme setting shows the theme you had (Modern dark on a fresh profile).
- On a non-English client: panel, section names and step text are translated, with no raw KEY_NAMES showing.

*Picker*
- Open the world map and a section: destinations list with times. Search "Ironforge": it appears with a time.
- Click a destination: the route shows. Spend or gain some gold, reopen a section: the times still make sense for your money.

*Routes*
- Stormwind to Ironforge: taxi route as before.
- From inside the Wizard's Sanctum, route to Stormwind's gates: walking at running speed (no mount speed); same out of Blackrock Mountain.
- A route with a "Use <item>" or "Cast <spell>" step (a class teleport, a teleport ring): the step and the navigator's button show the real spell or item name.

*Trip window*
- Start a trip: the window opens, tracks steps, and closes on arrival. An item on cooldown shows "Ready to use in N s", matching the route's wait.
- Stop the trip mid-route: the window closes, no errors.

*Map lines*
- With a route or trip, dots/dashes draw on the world map; a long list shows "N more above/below".
- Turn "Show route on map" off in settings: the lines disappear at once.

*Minimap lines*
- Start a trip with "Show route on minimap" on: lines draw round you. Zoom the minimap in and out, and cross a zone border: the lines keep the right scale.
- Stop the trip, then zoom the minimap and change zones: no zoom flicker (the events are only listened for during a trip).

*Hearthstone*
- On a character that bound before this update: route to the inn you are bound at: the hearthstone is still the first step (the old saved bind is the fallback).
- Bind at an inn: route to it: the hearthstone appears, reading "Use your Hearthstone to go to ...".
- On a second character that has not bound since this update: it is not offered the first character's inn (it falls back to the bind location text, or nothing). Bind it elsewhere, relog the first: each routes home to its own inn.
- `/dump MapzerothRebuildDB.hearthstone` after any bind: nil. `/dump MapzerothRebuildDB.hearthstones`: one entry per character that has bound.

*Flights*
- Open a flight master's window, then plan a flight: as before. The "assume flight points are found" setting still works.

#### Modern (WoW retail, Interface 120100)

*Login and settings*
- Log in: no Lua errors; the panel appears; `/mz settings` opens the page.
- No calendar window pops up at login (the addon opens the calendar quietly once). If one does, note it.

*Picker*
- Open a section: expansion pages (current expansion first, "Older content" below, Dungeons and Raids) with times.
- Search "Dornogal", "Orgrimmar": entries with times.

*Routes*
- Stormwind to Exodar: one portal step, no duplicate alternative.
- Alliance with the Garrison Hearthstone (toy 110560): a route to Lunarfall offers it; Horde is offered Frostwall only, never Lunarfall (and the reverse).
- A route with a hearthstone step reads "Use your Hearthstone to go to ...", not just "Use Hearthstone".
- A route across Eastern Kingdoms or Kalimdor still flies point to point, and never flies into or out of a Zidormi zone's past or present state.

*Trip window*
- As for Forever: steps, use-button names for toys and items, "Ready to use in N s", closes on arrival.

*Map lines* and *Minimap lines*
- As for Forever.

*Hearthstone*
- Bind at an inn: the hearthstone wording above. A second character does not inherit the first's bind (as for Forever).

*Holiday portals (only testable while the Darkmoon Faire is up)*
- Log in during the Faire, wait a few seconds, open the map: a route to the Faire offers the Darkmoon portal (Elwynn entrance for Alliance, Mulgore entrance for Horde).
- With the Faire down: no route ever offers a Darkmoon portal.
- Right after login, before doing anything else: a route must not show a holiday portal that isn't there.

*Zidormi zones*
- Route to Tirisfal, Vale, Uldum, Darkshore, Blasted Lands, Silithus, Dustwallow: no route offers the free ten-second hop between past and present, and none flies into the other phase and dead-ends.
- Standing in a Zidormi zone, plan a route out: walking and portals still work.

### Batch 3 — 2026-09-24

Done, one commit per finding on `rebuild` (not pushed): the batch 1 and 2 follow-ups, then 4a, 5 and 18. Both test runs green
after every commit: `python tests/harness.py` (29 files) and
`python tests/harness.py --toc Mapzeroth-Rebuild_Mainline.toc modern_smoke modern_picker`. No route, time, step text or
test expectation changed.

**Follow-ups.** `skin.strip` is gone. The validator serialises table-valued requirements by content (`{ 17, 628 }` compares
equal to itself); the Forever dataset validates clean and Modern reports exactly the same 25 `mapArtID` errors and 581 name
warnings as before (checked before and after). Both closures in `DistanceProvider` are file-level locals.

**Where the work differed from the brief, and why**

- **4a:** the brief's design, not the review's (a): the second build for the flight hint stays, and each build got cheap.
  Ground speed is asked of one outdoor and one indoor container per context (the indoor one only if the data has an indoor
  container), and edge tables are cached per (`World.generation`, outdoor speed, indoor speed), at most 4 keys, oldest evicted.
  `authored` and the two sample containers are cached per generation (`factsForWorld`). A cache miss calls
  `staticGeometryFor()`, so `staticBuildCount` still counts geometry passes and the new `materialiseCount` counts misses.
  Per-node adjacency order is unchanged (authored edges first, then geometry), so every search sees the same graph.
- **5, `Heading` and `distance`:** `Heading` does not call `distance` (it uses `MapPoint` and `yardsPerUnit`), so there was no
  duplicate distance to pass along. What buildModel did compute twice was `nodeOf(step.nodeID)`; that is now once.
- **5, jump check:** the last known spot carries its own projected world position (`active.last.world`, copied at the end of
  each update) so the jump check does not project it again. Without that an update projected the player twice, not once.
  The projection is inside a `pcall`, like `MapPoint`. Nothing of the current sample outlives an `Update`.
- **5, `yardsPerUnit`:** the memo is also keyed on the provider function in use, because tests replace the provider and the
  same mapID has a different scale in each. A map that could not be measured is not memoised. `MinimapLines` needed no change
  of its own: `draw` and `rebuild` only call `YardsPerUnit`.
- **18:** `zoneName` does not memoise a nil (a map the client cannot name yet is asked again). `Fold` takes `memo` as a second
  argument and `Prepare` passes it; `Search:ClearFoldMemo()` is called from `ClearNodeNameCache`. `Destinations:Build` is not
  cached across opens, as briefed.

**Numbers the tests observed** (test distances, so only the counts are meaningful; timings are the harness's, roughly)

- 4a, `test_travelgraph`: three builds and a `World:Build()` gave one geometry pass each time as before; `materialiseCount`
  stayed put for an equal-speed context, went up by one for riding skill 33388, by one for the re-built world, and a fifth
  distinct speed evicted the first (`materialiseCount` 14 and `staticBuildCount` 6 at the end of that test file).
  Modern, one graph: 33,899 edges; a build with a warm cache 0.036 s, 0.0024 s after; garbage kept alive by one extra graph
  13.6 MB, 1.0 MB after. Forever: 21,844 edges, 8.8 MB, 0.7 MB.
- 5, `test_navigation`: one update over a 6-step route makes 8 provider calls (the limit is steps + 2; unchanged from before,
  the saving is what each call costs). Client world projections for that update: 13 before, 5 after on the first tick with a
  heading (four of them the once-per-map scale), and 1 on every tick after (`test_travelgraph`: exactly 1).
  `YardsPerUnit` on a map: 2 provider calls the first time, 0 after.
- 18, Modern with a stub client: `C_Map.GetMapInfo` calls for one `Destinations:Build` 3,700 before and 890 after on the
  first map open, 1,271 before and 46 after on the next (the rest come from paths that ask for continents directly).

**Deferred on purpose**

- Finding 4 (a) as the review wrote it (`unfound` tag plus `opts.ignoreUnfound`, no second build) and (b) (the search reading
  the static triples itself): not part of this batch; the second build stays.
- Rebuilding entries only when faction, class or known spells change (finding 18): relevance can change between opens.
- `MinimapLines` still allocates its `toPixels` closure per redraw; not in the brief.
- The cached flyable-node list for `AddStart`/`AddDestination` (finding 12; batch 1's note said batch 3): not in this brief, so `isFlyable` still walks the tree per node there.
- Batches 4 and 5 not started.

### In-game checklist for batch 3

Batch 3 changes no behaviour: the checks are "nothing changed and it is smoother". Compare with what you saw for batches 1 and 2.

#### Forever (WoW Forever, Interface 16001)

- Stormwind to Ironforge: the same taxi route and time as in the batches 1 and 2 checklist (259 s at your riding skill).
- From inside the Wizard's Sanctum, route to Stormwind's gates: the same walking time as before.
- A route with a "Use <item>" or "Cast <spell>" step: the same step text and total time as before.
- Change your riding skill or shapeshift form (or learn a riding skill) and plan Stormwind to Ironforge on foot again: the walking time follows the new speed.
- Follow a trip for two minutes with the minimap route on: no frame-rate drop, the arrow and distance keep updating, the minimap lines keep their scale.
- Open the world map and a section several times: no hitch, the same destinations and times each time.

#### Modern (WoW retail, Interface 120100)

- Stormwind to Exodar: the same single portal step and time as before.
- The route to Lunarfall or Frostwall with the Garrison Hearthstone: the same step and time as before.
- A route across Eastern Kingdoms or Kalimdor (Stormwind to Ironforge on foot and by portal, or the one you used before): the same times, still flies point to point, still never into a Zidormi zone's other phase.
- Click a destination on the world map: the route appears with no hitch (this used to build the whole graph twice).
- Follow a trip for two minutes with the minimap route on: no frame-rate drop.
- `/mzr dumpgeometry` still works on retail (it computes the geometry fresh, not from the cache). Re-run it if you have not since batch 2, as noted above.

### Batch 4 — 2026-09-24

Batch 3 was checked first: both test runs green, and the claims that matter hold up in the code. Ground speed depends only
on a container's indoor flag, so caching by (outdoor, indoor) speed is sound. Nothing writes into a shared edge table: the
pathfinder copies before changing (`through`, `CollapseSteps`), and there is no assignment into a step in Journey,
Navigation, MapRoute or the UI. The Navigation scratch nodes keep the last and current samples' world positions apart.

Done, one commit per finding on `rebuild` (not pushed): 8, 7, 13, 10. Both test runs green after every commit. The Forever
data validates clean, and Modern still reports the same 25 `mapArtID` errors and 581 name warnings. The printed route
numbers are unchanged (Stormwind -> Ironforge 259 s, and the rest).

**Where the work differed from the brief, and why**

- **8:** the three tables went into a new hand-maintained `Data/<flavour>/Game.lua` for each flavour, not into existing
  files: Forever's `Pois.lua` and Modern's `Edges.lua` are generated, and none of the hand-maintained files was a natural
  home for classes and perks. `CLASS_TOKENS` is defined in both files instead of being optional, and Modern's lists its 13
  classes. `HOLIDAYS` (Modern only) and `FREQUENT_FLIER` (Forever only) are optional, as the engine already treated them.
  `gen_modern_edges.py` now points at the new file.
- **7:** it's `addon.PICKER_LAYOUT` (upper case, like `CURRENT_EXPANSION`): Forever's is in Game.lua, Modern's in
  Places.lua. `MovementSpeed` now states the rule ("riding data wins when present") as an explicit if/else, and Forever's
  RidingSkills.lua sets `DEFAULT_MOUNT_BONUS = nil` with a comment. The validator now errors on an unknown layout, on
  `"expansions"` with no `CURRENT_EXPANSION`, and on riding data and a flat bonus set together. The new test_sections
  check fails on the old code (checked).
- **13:** `addon.METHODS` in Constants.lua gives each method `kind`, `style`, `screens` and `ability`, and
  `addon:Method(m)` falls back to walk. `KINDS`, `STYLES`, `ABILITY_METHODS` and `DEFAULT_LOADING_SCREENS` are gone.
  Themes now colour by style (`styles = { foot, flight, boat, ability, default }`), and `Theme:MethodColor` goes through the
  style. The colours are the same values as before. The validator errors on an edge or ability method that isn't in
  `METHODS`, and neither dataset has one. **Visible changes, all small:** an equip step now draws in the ability style
  (gold, dotted) on the map and minimap instead of the on-foot style, and its panel marker was already gold. The panel
  markers for `transition`, `fly` and `phaseswitch` steps take the on-foot colour instead of the theme's grey default.
- **10:** the rule is one function, `FlightKnowledge.Usable(isFound, assume)`, and `FlightKnowledge.AnyFlight` is the
  shared "every flight" predicate. The flight hint's free route sets `flightUsable = AnyFlight`, and a context whose
  `flightUsable` already is `AnyFlight` builds no second graph. The harness's `makeCtx` defaults to that ("all") and takes
  `flights = fn, assumeFlights = bool`. `setFlights(ctx, fn, assume)` replaces the tests that assigned `flightNodeFound`
  by hand. **Outside the repo:** `tools/MapzerothDataTools/Dev.lua` `/mzr route ... allflights` set `flightNodeFound = nil`,
  which would now do nothing, so it now sets `flightUsable = a.FlightKnowledge.AnyFlight`.

**Deferred on purpose**

- Batch 5 (findings 6, 3 long-term, 4b): design-level, waiting on decisions (taken 2026-09-24, see batch 5).

### In-game checklist for batch 4

Only the route-drawing colours (finding 13) and the flight rule (finding 10) can show a difference.

#### Forever (WoW Forever, Interface 16001)

- Log in: no Lua errors. `/mz settings` opens.
- Stormwind to Ironforge: taxi, 259 s at your riding skill, as before. Learn or unlearn a riding skill: the walking times follow it.
- The picker's main page: Relevant, Cities, Towns, as before.
- With "assume flight points are found" **on**: a route through a flight point you have never opened a window at is
  offered, and the plan says it assumed it. With it **off**: that flight is left out, and the "a flight you haven't found
  would save N" hint appears when one would be quicker.
- At a flight master: a point the window shows as not found is never flown to, with the setting on or off.
- Plan a route with an equip step (a teleport cloak or ring you are not wearing): on the map and minimap the equip step's
  marker and line are gold like the teleport. Other lines and markers keep their colours in both themes.
- `/mzr route TAXI_2 TAXI_8 allflights`: routes as if every flight point were found.

#### Modern (WoW retail, Interface 120100)

- Log in: no Lua errors. The picker's main page is still the expansion page (current expansion, Dungeons, Raids, Older content).
- Walking times as before (flat mount bonus, 14 yd/s outdoors).
- The same flight-setting checks as for Forever.
- Stormwind to Exodar: one portal step, the same time as before (the loading-screen count is from the new table).
- Switch between Classic and Modern Dark themes with a route on the map: on-foot, flight, boat and portal lines keep their colours.

### Batch 5 — 2026-09-24

Decisions (user): drop 4b; wire up the phase handling (3, long-term); do the suffix fallback of 6 now. Also for 6: the
two flavours should not define nodes differently, and Modern's data is to move onto Forever's schema, not the reverse.
One commit each on `rebuild` (not pushed). Both test runs green. The Forever data validates clean. **Modern now
validates with 0 errors** (the 25 `mapArtID` errors are gone) and the same 581 name warnings.

**4b: dropped.** After 4a a Modern graph builds in 2.4 ms and an extra graph keeps about 1 MB alive, which isn't worth the
change to step shape through Pathfinder and Journey.

**3, long-term: phases.** It follows DESIGN.md section 2, with an "as built" note added there.
- Data: `gen_modern_nodes.py` tags every `_art<N>` container `{ phaseGroup, phaseSide = art id, phaseMap }`. Groups are
  named from the Zidormi nodes (`darkshore`, `tirisfal`, `uldum`, `vale`, `blasted_lands`, `silithus`, `dustwallow`),
  and any other phase node joins the group whose side has its art id. So Teldrassil (art 67 on maps 57 and 89) is a
  Darkshore-past side read from map 62's art, without the old `phaseCheckMapID` and without nesting it.
  `gen_modern_edges.py` turns the old `mapArtID` requirement into `inPhase = { group, art }` (18 edges), and gives the
  seven Zidormi edges `overridesPhase` in place of the batch 2 gate.
- Engine: `ctx.mapArtID` (`C_Map.GetMapArtID`) feeds `World:LivePhases`, which `Journey:Build` stores as `graph.phase`.
  Every search of the session starts from it, and the player's start and a waypoint on a split map (Darkshore) sit in
  the container for their side (`World:GetContainerForMap(mapID, phases)`). The fly mesh includes phased nodes again,
  but never joins two sides of one group. The temporary `_art` exclusion is gone, and so is
  `tools/strip_geometry_phase_fly.py`.
- **Where it differs from the design doc, and why:** a group the client can't tell apart is left out of the start state
  and is open (either side, nothing recorded). That covers past and present Tirisfal, Uldum and the Vale, which are two
  maps, each always showing its own art. **A route takes at most one phase switch.** The first version made unknown groups
  sticky, and the Modern search forked into up to 2^7 phase states and ran for minutes. Now it's 0.05 s against 0.025 s
  to price every destination with four groups known (test distances).
- The validator checks `inPhase` and `overridesPhase` against the defined groups.

**6, part: suffix fallback.** `addon:NodeKindFromID` reads the kind from either end of an id, skipping a trailing
faction, level or phase (`DALARAN_PALADIN_PORTAL_HORDE` is a portal; `DARK_PORTAL_SHADOWMOON_VALLEY` is not).
`Destinations`' `groupOf` uses it, so 121 Modern nodes are now `transport` instead of `other`. `NodeNames` uses it for
docks, zeppelins and trams ("Boralus Harbor"). Portals keep being named by where they lead ("Orgrimmar Portal"). A Zidormi step now reads "Talk to Zidormi: <zone> in the other time"
(`STEP_PHASESWITCH`, with machine-quality starter lines in every locale); before, it would have been "Go to <zone>".

**Still open**

- **Re-dump the Modern geometry** (`/mzr dumpgeometry` in retail). The shipped file still has no fly edges into or out
  of phased nodes (batch 2 stripped them). Its node count matches, so it is served as is, and phased places are reached
  only by their authored edges until then.
- Moving Modern onto Forever's schema (the rest of 6): done, see below.

**6, the rest: one schema for both flavours.** User's rule: no flavour-specific graph code because data is shaped
differently; Modern's data changes to fit Forever's schema, not the reverse. Differences that stay are real differences
in the game, expressed as data: Forever has no flying, so its walled cities have entrance nodes and its travel is mostly
taxi edges; Modern flies, so it has neither and relies on the fly mesh.
- **Cities.** Modern's `CityPlaces` is gone. `tools/modern_manual.py` `CITIES` feeds `gen_modern_nodes.py`, which writes
  `Data/Modern/Settlements.lua`: `addon.Cities[key]` in Forever's shape (`mapID, x, y, faction`, plus optional `maps`,
  `expansion`, `hub`, `taxi`), `addon.Towns = {}`, and a `CITY_<KEY>` centre node (`kind = "settlement"`) for each. The
  centre is the average of the city's outdoor nodes on its own map (user's call), or of hand-listed nodes for a city that
  is part of a zone's map (the two Pandaria shrines, named by their flight masters). `Destinations` has one settlement
  path: arrive by a walled city's entrances, else by its centre. A settlement falls back to its map's name. Two cities
  of one name get their continent. **Visible on Modern:** a city is now routed to its centre (before: to whichever of
  its nodes was nearest), and Shrine of Seven Stars is listed (its own map had no node, so it was left out before).
- **Ids.** `tools/modern_ids.py` is the one rule, and `gen_modern_nodes.py` writes the whole map
  (`tools/modern_source/id_map.tsv`) for the edges and abilities generators and for hand-written data. A kind leads:
  `DOCK_BORALUS`, `PORTAL_STORMWIND_BORALUS`, `ZEPPELIN_...`, `TRAM_SW`, and `FLIGHT_<PLACE>` for a flight master
  with no TaxiNodes id. 1024 ids changed. The shipped `Geometry.lua` was rewritten with the same map (it is ignored
  anyway until re-dumped: the node count changed).
- **Instances.** They are `INSTANCE_<NAME>` nodes with `kind = "instance"`, `journal = <journalInstanceID>` and, for a
  one-faction entrance, `faction`, the same as Forever's `INSTANCE_<NAME>`/`kind` (Forever's have `area` and no journal
  id). `InstanceNodeAliases` moved into `modern_manual.INSTANCE_JOURNALS`. `GetInstanceRef` reads the node's fields.
- **Engine.** `addon:NodeKindFromID` reads the prefix only (the suffix fallback is gone again). A `PORTAL_` node is
  named by where it leads in both flavours. **Visible on Forever:** Stormwind's portal to Dalaran now reads "Dalaran
  Portal" (was "Stormwind Portal"). The validator accepts kind-named ids (dock, portal, flight, ...) as named, and a
  journal id as an instance's name source.
- Modern validates with 0 errors and 207 "no name" warnings (the list below). Forever still validates clean.

**Modern data to gather** (what the flavours' shared schema expects that Modern doesn't have yet)

1. **Names for 207 nodes.** Done for 99: `tools/match_modern_area_nodes.py` matches the old English names against retail's
   AreaTable (with UiMap and UiMapAssignment from wago.tools, so an area must be in the node's zone and on its
   continent), and `gen_modern_nodes.py` writes `area = <id>`, so the client names them in every language. The 108 left
   (`tools/modern_source/area_node_matches.tsv`, verdict `zero`) are mostly generic spots (portal rooms, entrances, the
   hand-added doors) and the 14 Zidormi nodes, which read fine as their zone's name. A few real places the table
   doesn't have under the old name (Acherus, Stonard, Theramore, Magni's Camp, Peak of Serenity, the Shadowlands
   covenant spots) can be fixed with `/mzdump area` at the spot and `modern_manual.AREA_OVERRIDES`. Low priority.
2. **Flight masters without a TaxiNodes id.** Mostly done: `match_modern_taxi_nodes.py` only tried ids ending in
   `_FLIGHT`, so every faction or phase variant (`EVERLOOK_FLIGHT_HORDE`) was never matched. It now takes any `_FLIGHT`
   id, keeps the rows of the faction the id names (TaxiNodes Flags: 1 Alliance, 2 Horde), and settles what the name
   leaves open with in-game captures (`/mzdump nodes <mapID>` pasted into `tools/modern_source/taxi_captures/`, matched
   by map position). 39 more are `TAXI_<id>` now. 10 `FLIGHT_` nodes are left: the four Vaults of Atal'Utek ones
   (their own network, not taxis), and six duplicates of a point another node already is (past and present Lor'danel
   and Grove of the Ancients are one flight point each; Serpent's Spine 1073 serves both factions). All the
   continent captures are in (Kalimdor 12, Eastern Kingdoms 13, Outland 101, Northrend 113, Pandaria 424, Broken Isles
   619, Kul Tiras 876, Zandalar 875, Shadowlands 1550, Dragon Isles 1978, Khaz Algar 2274, K'aresh 2472). 7 names still
   match several ids, on purpose: Elysian Hold (Bastion's covenant sanctum: the Shadowlands capture doesn't list it), the
   Vashj'ir camps (the capture reports them on Vashj'ir's own map, our nodes are on its sub-zones, and both faction twins
   are listed), and the old Burning Crusade Tranquillien and Fairbreeze (below). A capture's coordinates can be
   continent-relative or negative (the client couldn't place the flight master in a zone); such an entry still settles
   a match by id. Four collisions remain: two nodes for one flight point (past and present Lor'danel, past and present
   Grove of the Ancients, the two Amani Foothold nodes, Lunarfall's two); the second keeps its source id.
   **The Burning Crusade Quel'Thalas is live** (checked in game 2026-09-24): old Eversong, Ghostlands and Silvermoon
   (maps 94, 95, 110) are a region of their own beside Midnight's, entered by portal. Orgrimmar's portal room and the
   Tirisfal reach old Silvermoon (present Tirisfal's Ruins of Lordaeron and past Tirisfal's Balnir Farmstead each have a
   portal in; old Silvermoon's one portal back goes to whichever Tirisfal the player is in: `inPhase` on each way); EPL's Quel'Lithien Lodge reaches the old Ghostlands (both ways;
   the Zidormi on each side is a teleporter to the same spot, not a phase switch). The old data authored the old and
   new Silvermoon and Fairbreeze flight masters under one id each; the generator used to keep only Midnight's.
   `modern_manual.SECOND_COPY_IDS` keeps both now, `CONFIRMED_TAXI_IDS` pins old Silvermoon (82) and old Fairbreeze
   (625), `NODE_PLACES` moves Falconwing Square (631) into old Eversong where the capture puts it. Midnight's Silvermoon
   has two flight masters: the Sanctum of Light (3131, the old data's) and the Royal Exchange (3132, hand-added, Horde
   only). A node may carry its own `faction`; `GetFlightOwner` reads it before the flights into the node. The region is shut off since Midnight: its only ways in are EPL's portal, the Horde portals
   from Orgrimmar and Tirisfal, and Shattrath's portal to the Isle of Quel'Danas (one way; now from the portal's own
   spot, `PORTAL_SHATTRATH_QUELDANAS`, landing where the capture says). The pre-Midnight flights Northpass Tower ->
   Zul'Aman and Shattered Sun Staging Area -> Light's Hope Chapel are dropped (`DROP_EDGES`). The Dazar'alor portal room's
   portal lands in the old Silvermoon too, and so does the mage's 32272, now "Teleport: Silvermoon (Burning Crusade)".
   Midnight's Silvermoon has its own teleport, 1259190 "Teleport: Silvermoon City" (`modern_manual.TELEPORTS`, for spells
   the old data lacks); it lands in Midnight Silvermoon's portal room.
3. **Inns (and towns).** `addon.Towns` is empty, so a Modern character's hearthstone can never resolve (known before).
   Forever's Wowhead pipeline (`tools/wowhead_scrape.js`, `gen_pois.py`) against retail's NPC database would give inns,
   and optionally banks, auction houses and trainers, in the same `Pois` shape.
4. **Landing spots that duplicate a city.** Done: every node on a city's map (or listed for it) carries `city = <key>`,
   as Forever's city POIs do, and a city-tagged node with no kind of its own is offered through the city, not listed on
   its own. Silvermoon's Wayfarer's Rest is `kind = "inn"` (`modern_manual.NODE_KINDS`), Modern's first inn: a
   hearthstone bound there resolves.
5. **Four old instance nodes with no journal match** (`STRATHOLME_DUNGEON`, `DIRE_MAUL_DUNGEON`,
   `SUNKEN_TEMPLE_DUNGEON`, `TEMPEST_KEEP_RAID`), next to the hand-added entrances. Stratholme's is where the pet-battle
   portals land. They need a look in game: drop them, or rename them as the landing spots they are.
6. **Re-dump the geometry** (`/mzr dumpgeometry`), as above.

### In-game checklist for batch 5

#### Modern (WoW retail, Interface 120100)

Re-dump the geometry first (see above), then `/reload`.

- Stand in Darkshore in the **present** (Zidormi not used): a route to Darnassus goes through Zidormi (a step reading
  "Talk to Zidormi: Darkshore in the other time", then Lor'danel, the flight to Rut'theran, and Darnassus), and never offers the Stormwind to
  Rut'theran portal.
- Switch Darkshore to the **past** with Zidormi, `/reload`, and plan to Darnassus again: no Zidormi step, and the
  Stormwind portal to Rut'theran may appear.
- Blasted Lands **present**: a route to Outland does not use the Stormwind portal straight to Outland. In the
  **past**: it may.
- Stand in Tirisfal, Uldum or the Vale: routes out of the zone work, and a destination there never needs Zidormi
  twice in one route.
- The picker search for "Boralus": the dock is listed and reads "... Harbor", not just the zone name.
- The picker's cities: each city routes to its middle. Stormwind to Dornogal, Valdrakken, Oribos: times look sane, and
  the trip ends mid-city. Shrine of Seven Stars (Alliance) and Shrine of Two Moons (Horde) are listed and routed to the
  shrine itself, not the middle of the Vale.
- Dungeons and raids are named from the Dungeon Journal as before, and the one-faction entrances (Siege of Boralus,
  Motherlode, Battle of Dazar'alor) are offered to their faction only.
- A route with a portal step still names the portal by where it leads.

#### Forever

- Forever's data is unchanged. One visible change: a portal is named by where it leads, so the Wizard's Sanctum portal
  reads "Dalaran Portal" (was "Stormwind Portal"). Once the servers are back: log in, open the picker, plan Stormwind to
  Ironforge and Stormwind to Dalaran, and check the portal step's wording.
